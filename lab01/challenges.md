# Challenges Log — Lab 01

Quick raw notes. No formatting needed — just write what happened, when it happened.
I'll help turn this into a polished section later.

- Used a placeholder GitHub URL (YOUR-USERNAME) when setting the remote, so `git push` failed with "repository not found." Fixed it with `git remote set-url origin <correct-url>` and confirmed with `git remote -v` before pushing again.

- 

-
